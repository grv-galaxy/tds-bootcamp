name: My First Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f1000758@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Finished
        run: echo "Workflow completed!"
